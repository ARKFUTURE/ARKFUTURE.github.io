image: debian:stable

pages:
  stage: deploy
  script:
    - apt-get update && apt-get full-upgrade -y
    - apt-get install -y ca-certificates
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - main
